function double(numbers) {
  return numbers.map((numbers) => numbers * 2);
}

//console.log(doubledDigits); // => [2, 4, 6]
// Do not remove the following line, it is for tests
module.exports = double;
